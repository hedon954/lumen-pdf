---
description: PRD 与 TDD 必须成对更新，并保持双向链接
alwaysApply: true
---

# PRD / TDD 维护

用户可感知行为、交互规则或模块边界发生变化时，同一 PR 必须更新或新增成对的：

- `docs/prd/prd-YYYY-MM-DD-<topic>.md`
- `docs/tdd/tdd-YYYY-MM-DD-<topic>.md`

两份文档都要有 `## 文档关系`，包含对应文档、前序、后续和 `docs/README.md`。若新规则改写旧需求，在旧文档原条款旁标注「后续修订」并链接新文档，同时更新 `docs/README.md` 配对表与主题演进。

不要只改代码或只写 CHANGELOG。完整规则见 `CLAUDE.md` 的「PRD 与 TDD」。
