---
description: LumenPDF 项目总览：技术栈、架构分层、核心约束
alwaysApply: true
---

# LumenPDF 项目总览

## 技术栈

- **前端**: SwiftUI + PDFKit（macOS 13+）
- **桥接**: Mozilla UniFFI（自动生成 Swift 绑定）
- **后端**: Rust + Tokio + SQLite（rusqlite + r2d2）
- **HTTP**: reqwest（LLM 调用 + MyMemory 兜底翻译）
- **TTS**: AVSpeechSynthesizer（本地发音，Swift 侧，无需 Rust）

## 架构分层（硬约束）

```
Swift UI  →  BridgeService  →  UniFFI  →  Rust interfaces  →  domain  ←  infrastructure
```

| 层                  | 目录                  | 约束                                   |
| ------------------- | --------------------- | -------------------------------------- |
| Swift UI            | `LumenPDF/`           | 不直接访问 DB、不发 HTTP；跨语言走 `BridgeService` |
| Rust interfaces     | `src/interfaces/`     | UniFFI export + 组装 SQLite / LLM      |
| Rust domain         | `src/domain/`         | 零外部 I/O 依赖（无 reqwest/rusqlite） |
| Rust infrastructure | `src/infrastructure/` | 实现 domain traits，不含业务逻辑       |

没有 `application/` 用例层。

## 关键文件

- 文档索引: `docs/README.md`（当前主题头、配对表）
- 基线 PRD: `docs/prd/prd-2026-03-22.md`
- 基线 TDD: `docs/tdd/tdd-2026-03-22.md`
- UniFFI 入口: `lumen-pdf-core/src/interfaces/api.rs`
- 构建脚本: `scripts/build-rust.sh`

## PRD 与 TDD

用户可感知行为或模块边界变化时，同一 PR 必须**新增**成对的 PRD 与 TDD，用 frontmatter 的 `prev` / `next` 接到对应主题链。祖先只改 `next`。`docs/plan/` 不进规格链。详见 `CLAUDE.md` 的「PRD 与 TDD」和 `docs/README.md`。

## 翻译降级策略

本地缓存（SQLite）→ LLM（OpenAI 兼容）→ MyMemory API（兜底）
