---
description: Rust 分层：domain 零 I/O，interfaces 组装，infrastructure 实现
globs: **/*.rs
alwaysApply: false
---

# Rust 分层

### domain/

零 I/O。实体、trait、翻译 domain service。禁止 `reqwest` / `rusqlite` / `r2d2`。

### infrastructure/

实现 domain trait。不把业务规则写在 HTTP/SQL 里。仓库文件名：`{entity}_repo.rs`。

### interfaces/

`#[uniffi::export]` + 组装 SQLite / LLM。笔记/单词/文库直接调仓库。单词翻译走 `TranslationDomainService`；句子和导读按现有行为直连 `LlmTranslator`。

## Domain 测试

每个 `domain/*/service.rs` 要有无 I/O 的 `#[cfg(test)]`。用内联 `Fake*`，不用 mockall。覆盖缓存命中、LLM 成功写缓存、失败走兜底、兜底不写缓存。`cargo test domain`。

错误统一 `LumenError`，生产代码不用 `unwrap()`。
