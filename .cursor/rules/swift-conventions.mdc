---
description: Swift：MainActor、BridgeService、PDFKit
globs: **/*.swift
alwaysApply: false
---

# Swift

- UI 更新必须在 `@MainActor`
- 跨语言只走 `BridgeService`；不要在 View 里调 UniFFI 顶层函数，也不要 `URLSession` / `sqlite3`
- 删笔记/单词若需要同步去掉划线/高亮，用 `BridgeService.deleteNoteRemovingUnderline` / `deleteVocabularyRemovingHighlight`
- 句子提取在 Swift 完成；标注自定义属性 key：`lumen_entry_id`
- 视口保存/恢复在 `PDFKitView` Coordinator 与 `ReadingRestorationStore`，不要再引入平行的位置服务

Async UniFFI 用 `await`。长操作放 `Task`。
