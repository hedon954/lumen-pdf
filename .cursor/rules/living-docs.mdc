---
description: 当前规格维护 product / architecture，演进追加 history.md 一行
alwaysApply: true
---

# 文档

用户可感知行为变化时改 `docs/product.md`。模块边界、持久化、桥接、算法变化时改 `docs/architecture.md`。同一 PR 还要在 `docs/history.md` 表末**追加一行**（日期、一句话、不必新建归档文件）。

不要新增 `docs/prd/` 或 `docs/tdd/` 文件。不要改 `docs/archive/`。`CHANGELOG.md` 只写发布说明，不是演进时间线。
