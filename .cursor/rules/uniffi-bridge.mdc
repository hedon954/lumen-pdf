---
description: UniFFI 桥接：proc-macro 导出，无 UDL
globs: "**/api.rs"
alwaysApply: false
---

# UniFFI

Proc-macro only（`uniffi::setup_scaffolding!()`）。没有 `.udl`。

1. `interfaces/api.rs` 加 `#[uniffi::export]`（async 用 `async_runtime = "tokio"`）
2. 数据类型 `#[uniffi::Record]`，错误 `#[uniffi::Error]`
3. `make build-rust` 生成 `LumenPDF/Generated/`（不要手改）
4. `BridgeService.swift` 包一层

全局：`POOL`、`LLM_CONFIG`。不要另起 Tokio runtime。
